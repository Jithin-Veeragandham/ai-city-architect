from local_search import *


def main():
    best_grid, shortest_paths = hill_climbing(max_iterations=1000)


if __name__ == "__main__":
    """
    Entry point for the script. Executes the `main` function when the script is run directly.
    """
    main()